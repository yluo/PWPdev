%%% Loads NCEP forcing


%%% if Forcing.mat exists, then loads from file, else extract forcing from
%%% ncep archive
%%% note: currently only 2000-mid2011 available...

try
    load forcing.mat;
catch
    F = get_forcing(ncep_path,lat_float(5,:),lon_float(5,:),[yrstart yrstop]);
end
latitude = laFLT;